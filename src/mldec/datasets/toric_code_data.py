

def uniform_over_good_examples(n, config):
    """Create a uniform distribution over good examples in the toric code
    config contents:

    """


def create_dataset_training(n, config):
    """Create a set of 'true' weights to be sampled for a training set.

    config contents:
    
    """


def sample_virtual_XY(train_weights, batch_size, n, config):
    """Sample a virtual dataset of size `batch_size` from the true weights.

    config contents:

    """